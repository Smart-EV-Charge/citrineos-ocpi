/**
 * ADAPTER-0007 — sequelize-cli bridge deleted.
 * Ongoing OCPI migrate against the CitrineOS `citrine` database is retired.
 * One-shot trigger drop: apply migrations/20260802120000_drop_notification_triggers.sql
 * with an ops-held citrine credential, not via this package's runtime config.
 */
module.exports = {
  config: null,
  'models-path': null,
  'seeders-path': null,
  'migrations-path': null,
};
